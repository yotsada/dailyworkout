27/04/2569 02:21 ออกแบบ home page ใหม่เป็น 4-tile grid (TODAY, clock, schedule, setting) ตามดีไซน์ที่ส่งให้, schedule เชื่อมกับหน้า profile, เพิ่ม 3D button effect (.btn-3d) ให้ทุกปุ่มทุกหน้า
27/04/2569 02:26 สร้างหน้า setting มีปุ่ม logout, เชื่อม setting tile จาก home
27/04/2569 02:32 - TiltButton Gum style (tilt+glare+depth) สำหรับ SIGNUP, LOGIN, LOGOUT
                 - กรอบ home tiles และ profile pills บางลง (border-2 → border)
27/04/2569 02:40 - เปลี่ยน theme เป็นขาวแดงทั้งแอป
27/04/2569 02:48 - login: clock + "or" เป็นสีแดง
                 - home: clock ใหญ่ไม่มีกรอบ, ปุ่ม TODAY/SCHEDULE/SETTING แดงตัวหนังสือขาว
                 - profile: เปลี่ยนจาก icon pill เป็นปุ่มแดง สร้างใหม่/นำเข้า
27/04/2569       - home: ย้อนกลับเป็น 4-tile grid (TODAY/clock/schedule/setting)
                 - SVG icons: dumbbell (TODAY), clipboard (schedule), gear (setting)
                 - สีขาวดำ, border บาง, tile สี่เหลี่ยมจัตุรัส
                 - เปลี่ยน tile ทุกอันเป็น TiltButton (tile mode) สไตล์เดียวกับ login
                 - profile: ปุ่ม pill shape icon-only (+ circle, download) ใช้ TiltButton pill+tile mode
                 - login: Google button เปลี่ยนเป็น TiltButton pill สีขาว/เทา, เพิ่ม surface/side/textColor props ให้ TiltButton
                 - sides: #7f1d1d, btn-3d shadow: #991b1b, TiltButton: #dc2626/#991b1b
                 - borders/icons/labels ทุกหน้าเป็นโทนแดง
29/04/2569       - profile: AddProfileModal — กรอกชื่อโปรไฟล์, ลากอุปกรณ์จาก grid → drop zone "I HAVE"
                 - BODY ติดอยู่ใน I HAVE เสมอ, อุปกรณ์ที่ลากไปแล้ว dim ใน grid, กด × เพื่อเอาออก
                 - โปรไฟล์บันทึกลง localStorage, กดปุ่ม + เพื่อเปิด popup
29/04/2569       - profile: โปรไฟล์แสดงเหนือปุ่ม + / download
                 - เพิ่ม toggle switch (iOS style) เลือก active profile ทีละหนึ่ง
                 - activeProfileId เก็บใน localStorage, การ์ด active ขอบ/พื้นหลังแดง
29/04/2569 03:26 - profile: แก้ toggle และการ์ดโปรไฟล์เปลี่ยนเป็นสีเขียวระหว่าง transition
                 - สาเหตุ: Tailwind transition-colors ขัดกับ background ที่ set ผ่าน inline style
                 - แก้: ลบ transition-colors/transition-transform class ออก ใส่ transition ใน style object แทน
29/04/2569 03:30 - profile: แก้ toggle knob หลุดออกนอก track
                 - สาเหตุ: span ไม่มี left กำหนดชัด browser วาง knob ผิดตำแหน่ง
                 - แก้: ใช้ position absolute + top:2 left:2 translateX(0/20px) ลบ Tailwind class ออกทั้งหมด
                 - เปลี่ยนสี on จากแดงเป็นเขียว (#16a34a / #15803d)
29/04/2569 03:41 - backend: เพิ่ม Equipment model (id, name) ใน Prisma schema
                 - สร้าง equipment module (GET /equipment), seed 10 รายการ
                 - seed: Dumbbell, Barbell, Resistance Band, Kettlebell, Pull-up Bar, Jump Rope, Bench, Yoga Mat, Cable Machine, Treadmill
29/04/2569 03:41 - profile/AddProfileModal: fetch อุปกรณ์จาก GET /equipment แทน hardcode
                 - ไอคอนทุกชิ้นเปลี่ยนเป็น ImagePlaceholderIcon (รูป placeholder ชั่วคราว)
                 - modal สูง 90dvh, slide ขึ้นจากล่าง (mobile), equipment grid flex-1 scroll อิสระ
29/04/2569 03:57 - AddProfileModal: เพิ่มปุ่มปิด (X) มุมบนขวา
                 - Body เข้า DB (seed 11 รายการ), ใช้ ImagePlaceholderIcon เหมือนกันทุกชิ้น
                 - แก้ขนาดการ์ดไม่ match: ลบ Tailwind class ออกทั้งหมด ใช้ inline style ล้วน + width ตรงทั้ง outer/inner
                 - บันทึก equipment เป็น name แทน cuid, profile page ใช้ .toUpperCase() แทน EQUIPMENT_LABELS
29/04/2569 03:59 - profile: กดการ์ดโปรไฟล์ → ProfileActionModal popup มีแค่ปุ่มถังขยะลบ profile
                 - toggle ใช้ stopPropagation ไม่ให้ trigger card click
                 - ลบแล้วเคลียร์ activeProfileId ด้วยถ้า active อยู่
29/04/2569 04:05 - profile: ProfileActionModal ปรับขนาดเท่า AddProfileModal (90dvh, slide from bottom)
                 - ปุ่มถังขยะเปลี่ยนเป็น TiltButton (3D style) centered ใน popup
                 - แสดงชื่อโปรไฟล์เหนือปุ่ม, มีปุ่ก X ปิด
29/04/2569 21:26 - แก้ DayWheel tap offset ผิด (วัดจากขอบบน → วัดจากกึ่งกลาง slot)
29/04/2569 21:32 - profiles: ผูก profile กับ user — POST/GET /profiles ใช้ JwtAuthGuard, userId มาจาก JWT
                 - เพิ่ม DELETE /profiles/:id (deleteMany where id+userId)
                 - profile page: โหลด profiles จาก GET /profiles แทน localStorage
                 - activeProfileId key ด้วย userId (activeProfileId_<userId>) ป้องกัน cross-user leak
                 - handleDelete เรียก DELETE /profiles/:id ก่อนลบออกจาก state
29/04/2569 22:02 - User model: เพิ่ม role String @default("user")
                 - AdminGuard: ตรวจ req.user.role === 'admin', 403 ถ้าไม่ใช่
                 - AdminModule: POST /admin/users — สร้าง admin user (ต้อง login เป็น admin)
                 - POST /equipment, POST /exercises ต้อง JwtAuthGuard + AdminGuard
                 - auth response ส่ง role กลับมาด้วย, frontend เก็บใน localStorage user
                 - seed: สร้าง admin เริ่มต้น (admin / admin1234) ถ้ายังไม่มี
                 - setting page: แสดง admin section (Add Equipment, Add Exercise, Create Admin) เฉพาะ role=admin, toast confirm
                 - backend: เพิ่ม model Profile, ProfileEquipment, ProfileDay, Exercise, DayExercise, ExerciseSet
                 - seed exercise 70 ท่า ครบ 7 กลุ่มกล้ามเนื้อ × อุปกรณ์ที่มีอยู่
                 - backend: GET /exercises?bodyPart=&equipmentNames= (ExercisesModule)
                 - backend: POST /profiles, GET /profiles (ProfilesModule) บันทึกโปรไฟล์พร้อม schedule+workout ลง DB
                 - frontend AddProfileModal: เพิ่ม Step 3 (Workout) — วงล้อแนวนอน (DayWheelH), กด body part pill → โหลดท่าที่ตรงกับ equipment, กดท่าเพิ่มลง list พร้อม set/rep/rest editor (−/+ ต่อ set, toggle count/time)
                 - profile page: handleSave POST /profiles ก่อน fallback localStorage
